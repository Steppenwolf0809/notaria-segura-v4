{
  "name": "notaria-segura",
  "version": "1.0.0",
  "description": "Sistema de trazabilidad documental notarial",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "build": "npm run build:frontend && cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && npm run install:frontend && npm run install:backend",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:both": "start cmd /k \"npm run dev:backend\" && start cmd /k \"npm run dev:frontend\"",
    "start:backend": "cd backend && npm start",
    "setup:dev": "npm run install:all && echo '✅ Dependencias instaladas' && echo '💡 Crea backend/.env basándote en backend/.env.example' && echo '🚀 Luego ejecuta: npm run dev'"
  },
  "keywords": [
    "notaria",
    "trazabilidad",
    "documentos",
    "whatsapp"
  ],
  "author": "Notaría Segura",
  "license": "PRIVATE",
  "engines": {
    "node": "20.x",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "twilio": "^5.8.0"
  }
}
