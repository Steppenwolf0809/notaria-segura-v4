/**
 * Servicio para agregar resultados de extracción de múltiples fuentes
 * Este archivo es un stub - la implementación completa no está disponible
 */

class ExtractionAggregator {
  static async hybridExtract(options) {
    // Stub implementation - always return null to allow fallback
    console.log('⚠️ ExtractionAggregator.hybridExtract not implemented, using fallback')
    return null
  }
}

export default ExtractionAggregator