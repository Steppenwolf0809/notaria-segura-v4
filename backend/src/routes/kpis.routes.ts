import type { Request, Response } from 'express';
// Archivo TypeScript informativo para alineación con roadmap TS.
// La implementación en runtime está en backend/src/routes/kpis-routes.js
export const _meta = {
  note: 'Rutas KPIs – ver kpis-routes.js para implementación runtime',
};

